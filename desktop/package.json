{
  "name": "zaram-desktop",
  "version": "0.7.0",
  "private": true,
  "main": "dist/src/main/index.js",
  "scripts": {
    "dev": "tsc -p . && node start-electron.js",
    "build": "tsc -p .",
    "watch": "tsc -p . --watch",
    "lint": "echo \"No linter configured for desktop\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "cross-env": "^7.0.4",
    "electron": "^28.3.3",
    "electron-builder": "^24.9.1",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
