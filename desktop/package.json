{
  "name": "zaram-desktop",
  "version": "0.7.0",
  "private": true,
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "tsc -p . && electron .",
    "build": "tsc -p .",
    "watch": "tsc -p . --watch",
    "lint": "echo 'No linter configured for desktop'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "electron": "^28.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron-builder": "^24.9.1",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
