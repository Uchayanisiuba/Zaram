# AGENTS

This workspace is a full-stack project with:

- frontend: React + Vite + Tailwind
- backend: Python services and API entrypoints

Guidelines:

1. Make focused changes and avoid broad rewrites.
2. Preserve existing behavior unless the task explicitly requires a change.
3. Keep code readable and consistent with the surrounding files.
4. Validate syntax and relevant tests when possible.
5. If a task is ambiguous, ask for clarification before making structural changes.
