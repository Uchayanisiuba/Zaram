// Idle
<Orb state="idle" />
// CSS: gradient + breathing animation (scale 1.0→1.02, 4s)

// Listening
<Orb state="listening" />
// CSS: emerald gradient + ripple effect

// Thinking
<Orb state="thinking" />
// CSS: cyan gradient + particle swirl

// Executing
<Orb state="executing" />
// CSS: orange gradient + pulse

// Error
<Orb state="error" />
// CSS: red gradient + shake