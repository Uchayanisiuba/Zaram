import { useEffect } from 'react'
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import { Sidebar } from '@/components/layout/Sidebar'
import { Workspace } from '@/pages/Workspace'
import { Models } from '@/pages/Models'
import { VoiceStudio } from '@/pages/VoiceStudio'
import { Memory } from '@/pages/Memory'
import { KnowledgeVault } from '@/pages/KnowledgeVault'
import { useModelStore } from '@/stores/modelStore'

function App() {
  const { initializeMockData } = useModelStore()

  useEffect(() => {
    initializeMockData()
  }, [initializeMockData])

  return (
    <BrowserRouter>
      <div className="flex h-screen bg-background">
        <Sidebar />
        <div className="flex-1 flex flex-col overflow-hidden">
          <Routes>
            <Route path="/" element={<Workspace />} />
            <Route path="/models" element={<Models />} />
            <Route path="/voice-studio" element={<VoiceStudio />} />
            <Route path="/memory" element={<Memory />} />
            <Route path="/knowledge-vault" element={<KnowledgeVault />} />
            <Route path="/chat" element={<Workspace />} />
            <Route path="/agents" element={<Workspace />} />
            <Route path="/automation" element={<Workspace />} />
            <Route path="/marketplace" element={<Workspace />} />
            <Route path="/analytics" element={<Workspace />} />
            <Route path="/settings" element={<Workspace />} />
            <Route path="/developer" element={<Workspace />} />
            <Route path="/providers" element={<Workspace />} />
          </Routes>
        </div>
      </div>
    </BrowserRouter>
  )
}

export default App