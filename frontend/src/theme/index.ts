/**
 * This file serves as the central hub for exporting all design tokens.
 * It allows for a clean import path when consuming tokens in the Tailwind config
 * and for generating CSS variables.
 */

export * from './tokens';